#ifndef __FILE_IO_H__
#define __FILE_IO_H__

//TODO: make this be a common interface around PhysFS when on PC and something else on other platforms

class FileIO {
    class File {
    }

    public virtual File * openFile 
};

#endif